mod model;
mod repository;

use model::pool::Pool;
use model::vote::Vote;
use model::admin::Admin;

fn main() {
    println!("VoteBack - Backend Rust");
    println!("Models chargés avec succès !");
}